(()=>{
"use strict";
const VERSION="7.1.0",STORY_TYPE="lifeWheelBattleBridge",M=globalThis.JJKV7Model;if(!M)throw new Error("JJKV7Model must load before v7-data.js");
const LEGACY_TOJI_HEAVY=new Set(["card_inverted_spear_of_heaven_pierce","card_chain_of_thousand_miles_bind","card_soul_split_katana_slash"]),STAT_KEYS=Object.freeze({cursedEnergy:["cursedEnergyScore","cursedEnergy","ce"],control:["controlScore","control"],efficiency:["efficiencyScore","efficiency"],martial:["martialScore","martial"],body:["bodyScore","body"],talent:["talentScore","talent"]});
const num=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f,clamp=(v,lo,hi)=>Math.min(hi,Math.max(lo,num(v,0))),arr=v=>Array.isArray(v)?v:v==null?[]:[v];function clone(v){try{return structuredClone(v)}catch{try{return JSON.parse(JSON.stringify(v))}catch{return v&&typeof v==="object"?{...v}:v}}}function profileOf(v){return v?.characterCardProfile||v?.profile||v||{}}function profileText(v){const p=profileOf(v);return[v?.id,v?.name,p.id,p.characterId,p.displayName,p.name,p.visibleGrade,p.officialGrade,p.powerTier,p.notes,...arr(p.traits),...arr(p.innateTraits),...arr(p.cardTags),...arr(p.specialHandTags)].filter(Boolean).join(" ")}function isLifeWheelProfile(p){p=p||{};const t=[p.displayName,p.name,p.notes,p.source,p.sourceType].filter(Boolean).join(" ");return /^战力导入：/.test(String(p.displayName||p.name||""))||/life[-_ ]?wheel|人生转盘|转盘导入/i.test(t)||Boolean(p.encodedCombatProfile)}function isZeroCe(p){const f=p?.flags,ft=Array.isArray(f)?f.join(" "):f&&typeof f==="object"?Object.keys(f).filter(k=>f[k]).join(" "):String(f||"");return /zero.?ce|零咒力|天与咒缚（肉体强化型）|天与暴君|domainSureHitInvalid/i.test(`${profileText(p)} ${ft}`)}function readFromObject(o,names){if(!o||typeof o!=="object")return NaN;for(const name of names){if(!Object.prototype.hasOwnProperty.call(o,name))continue;const n=M.rankValue(o[name]);if(Number.isFinite(n))return n}return NaN}function readStat(p,id){p=p||{};for(const s of[p.raw,p.baseStats,p.stats,p]){const n=readFromObject(s,STAT_KEYS[id]||[id]);if(Number.isFinite(n))return n}return 4}function readTechniquePower(p){p=p||{};for(const v of[p.techniquePower,p.technique?.power,p.techniques?.[0]?.power,p.raw?.techniquePower]){const n=M.rankValue(v);if(Number.isFinite(n))return n}return 4}function transcendenceFor(p){if(!isLifeWheelProfile(p)||typeof state==="undefined")return{};const a=state.flags?.battleBridgeTranscendence||{},b=state.flags?.v7Transcendence||{},ids=["cursedEnergy","control","efficiency","martial","body","talent","techniquePower"];return Object.fromEntries(ids.map(id=>[id,Math.max(0,Math.trunc(num(b[id]??a[id],0)))]))}function model(p){p=p||{};return M.derive({cursedEnergy:readStat(p,"cursedEnergy"),control:readStat(p,"control"),efficiency:readStat(p,"efficiency"),martial:readStat(p,"martial"),body:readStat(p,"body"),talent:readStat(p,"talent"),techniquePower:readTechniquePower(p),zeroCe:isZeroCe(p),transcendence:transcendenceFor(p)})}
function actionText(a){return[a?.id,a?.actionId,a?.cardId,a?.name,a?.label,a?.type,a?.cardType,a?.damageType,a?.scalingProfile,a?.risk,a?.effectSummary,...arr(a?.tags),...arr(a?.specialHandTags)].filter(Boolean).join(" ").toLowerCase()}function actionTier(a){return M.tierFromText(actionText(a),a?.risk)}function actionArchetype(a){const t=actionText(a);if(/heal|healing|rct|reverse|治疗|疗伤|回复|support|resource|防御|guard|block/.test(t))return"utility";const p=/melee|physical|martial|体术|近身|拳|踢|咒具|cursed_tool|weapon|刀|剑|chain/.test(t),q=/technique|innate|术式|咒法|curse|beam|projectile|domain|领域|必中|blue|red|purple|苍|赫|茈|御厨子|shrine|maximum/.test(t);if(p&&q)return"hybrid";if(p)return"physical";return q?"technique":"generic"}function genericTechniqueMultiplier(p,a){const k=actionArchetype(a),m=model(p);if(k==="physical"||k==="utility")return 1;const f=Math.exp(.085*(m.techniqueOffense-4));return k==="hybrid"?Math.sqrt(Math.max(.45,f)):f}function directTechniqueMultiplier(actor,card){const w=globalThis.JJKBattleDataDirect?.getBattleCardCoreWeights?.(card)||{martial:0,technique:1},tw=clamp(w.technique,0,1);if(tw<=0)return 1;const m=model(profileOf(actor));return Math.exp(.07*(m.techniquePower-m.control)*tw)}function normalizeScaling(card){if(!card?.scaling)return card;const c=clone(card);c.scaling={...c.scaling};const s=c.scaling;s.bodyResistanceBase=.10;s.bodyResistancePerRank=.025;s.bodyResistanceMin=.025;s.bodyResistanceMax=.42;s.martialEvasionPerRank=.025;s.efficiencyCostPerRank=.07;s.efficiencyCostMin=.48;s.efficiencyCostMax=1.55;s.sourceDamageMin=Math.min(num(s.sourceDamageMin,.25),.25);s.sourceDamageMax=Math.max(num(s.sourceDamageMax,2.5),3.2);return c}function isStoryBattle(b){return String(b?.activityContext?.type||"")===STORY_TYPE}function isPlayerActor(a){return String(a?.side||"")==="left"||isLifeWheelProfile(profileOf(a))}function scaleDamageFields(a,m){m=num(m,1);for(const[o,k]of[[a,"damage"],[a,"baseDamage"],[a?.effect,"damage"]])if(o&&Number.isFinite(Number(o[k]))&&Number(o[k])>0)o[k]=Number((Number(o[k])*m).toFixed(4))}function neutralizeEmbeddedTojiHotfix(a,actor){const id=String(a?.id||a?.actionId||a?.cardId||"");if(!LEGACY_TOJI_HEAVY.has(id)||!/伏黑甚尔（怀玉|toji_fushiguro_hidden_inventory/i.test(profileText(actor)))return a;const c=clone(a);scaleDamageFields(c,1/.82);c.v7LegacyCompatibility="neutralized-toji-0.82";return c}function mutateResultHp(r,h,d){if(!r||typeof r!=="object")return;const seen=new Set,walk=o=>{if(!o||typeof o!=="object"||seen.has(o))return;seen.add(o);if("afterHp"in o&&Number.isFinite(Number(o.afterHp)))o.afterHp=Number(h.toFixed(1));if("defeated"in o)o.defeated=h<=0;if("overkill"in o&&h>0)o.overkill=0;Array.isArray(o)?o.forEach(walk):Object.values(o).forEach(walk)};walk(r);r.v7AppliedDamage=Number(d.toFixed(1))}
function installResourceModel(){const api=globalThis.JJKDuelResource;if(!api||typeof api.deriveDuelResourcesFromProfile!=="function")return false;if(api.deriveDuelResourcesFromProfile.__jjkV7Data)return true;const old=api.deriveDuelResourcesFromProfile,w=function(p,c,side){const r=old.call(this,p,c,side);if(!r)return r;const m=model(p);r.maxHp=m.maxHp;r.hp=m.maxHp;r.maxCe=m.maxCe;r.ce=m.maxCe;r.ceCostMultiplier=m.ceCostMultiplier;r.bodyResistance=m.bodyResistance;r.regenRatio=m.zeroCe?0:.02+.11*(1-Math.exp(-(m.control+m.efficiency)/16));r.ceRegen=m.zeroCe?0:Number((r.maxCe*r.regenRatio).toFixed(1));r.v7Model={...m};r.raw={...(r.raw||{}),cursedEnergyScore:m.cursedEnergy,controlScore:m.control,efficiencyScore:m.efficiency,martialScore:m.martial,bodyScore:m.body,talentScore:m.talent};return r};Object.defineProperty(w,"__jjkV7Data",{value:true});api.deriveDuelResourcesFromProfile=w;return true}function installDirectCardModel(){const api=globalThis.JJKBattleDataDirect;if(!api||typeof api.calculateBattleCardSettlement!=="function")return false;if(api.calculateBattleCardSettlement.__jjkV7Data)return true;const old=api.calculateBattleCardSettlement,w=function(card,actor,opponent,options){const c=normalizeScaling(card),r=old.call(this,c,actor,opponent,options);if(!r||num(r.damage,0)<=0)return r;const m=directTechniqueMultiplier(actor,c);if(Math.abs(m-1)<.002)return r;r.damage=Number((num(r.damage,0)*m).toFixed(1));if(r.damageBreakdown&&typeof r.damageBreakdown==="object")r.damageBreakdown.v7TechniquePowerMultiplier=Number(m.toFixed(4));r.v7TechniquePowerMultiplier=Number(m.toFixed(4));return r};Object.defineProperty(w,"__jjkV7Data",{value:true});try{globalThis.JJKBattleDataDirect=Object.freeze({...api,calculateBattleCardSettlement:w});return true}catch{try{api.calculateBattleCardSettlement=w;return true}catch{return false}}}function installActionModel(){const api=globalThis.JJKDuelActions;if(!api||typeof api.applyDuelActionEffect!=="function")return false;if(api.applyDuelActionEffect.__jjkV7Data)return true;const old=api.applyDuelActionEffect,w=function(action,actor,opponent,battle){let a=neutralizeEmbeddedTojiHotfix(action,actor),p=profileOf(actor);if(isStoryBattle(battle)&&isPlayerActor(actor)){a=clone(a);if(!a.scaling&&a.type!=="card_template_runtime"){const tier=actionTier(a),ratio=M.projectionRatio(tier);a.projectionRuntime={...(a.projectionRuntime||{}),active:true,damageSettlementRatio:ratio};a.projectionDamageSettlementRatio=ratio;const mult=genericTechniqueMultiplier(p,a);if(Math.abs(mult-1)>=.002){scaleDamageFields(a,mult);a.v7TechniquePowerMultiplier=Number(mult.toFixed(4))}}}const before=num(opponent?.hp,0),maxHp=Math.max(1,num(opponent?.maxHp,before||1)),r=old.call(this,a,actor,opponent,battle);if(!opponent||before<=0)return r;const after=num(opponent.hp,0),loss=Math.max(0,before-after);if(loss<=0)return r;const am=model(p),dm=model(profileOf(opponent)),tier=actionTier(a),soft=Math.min(before,M.softBurstDamage(loss,maxHp,tier,am.combatRating,dm.combatRating));if(soft+.5<loss){opponent.hp=Math.max(0,Number((before-soft).toFixed(1)));mutateResultHp(r,opponent.hp,soft);try{(battle?.resourceLog||battle?.log)?.unshift?.({side:opponent.side||"right",title:"V7 单行动爆发曲线",detail:`${tier==="ultimate"?"终结技":tier==="high"?"高输出":"常规行动"}原实际损失 ${loss.toFixed(1)}，统一软压缩后 ${soft.toFixed(1)}。无硬上限，超出部分仍继续增长。`,type:"system",delta:{v7BurstCurve:true,rawLoss:loss,softened:soft,tier}})}catch{}}return r};Object.defineProperty(w,"__jjkV7Data",{value:true});api.applyDuelActionEffect=w;return true}
function installAll(){return Object.freeze({resource:installResourceModel(),directCards:installDirectCardModel(),actions:installActionModel()})}function ready(r){return Boolean(r.resource&&r.directCards&&r.actions)}let attempts=0;function boot(){const report=installAll();globalThis.JJKV7Data.installReport=report;if(ready(report)){document.documentElement?.setAttribute("data-jjk-v7-data",VERSION);try{document.dispatchEvent(new CustomEvent("jjk:v7-data-ready",{detail:{version:VERSION,report}}))}catch{}return}attempts+=1;if(attempts<40)setTimeout(boot,100);else console.error("[JJK V7] data adapters did not fully install",report)}globalThis.JJKV7Data={version:VERSION,model,installAll,installReport:null,actionTier,actionArchetype};boot();
})();