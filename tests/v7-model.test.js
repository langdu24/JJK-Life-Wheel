const assert=require("node:assert/strict");const M=require("../core/v7-model.js");const approx=(a,e,t=1e-9)=>assert.ok(Math.abs(a-e)<=t,`${a} != ${e}`);assert.equal(M.VERSION,"7.1.0");assert.equal(M.rankValue("E-"),0);assert.equal(M.rankValue("B"),4);assert.equal(M.rankValue("EX-"),9);assert.equal(M.rankValue("EX"),10);approx(M.transcendStepValue(1),.24);assert.ok(M.transcendStepValue(2)<M.transcendStepValue(1));assert.ok(M.transcendStepValue(3)<M.transcendStepValue(2));assert.ok(M.transcendTotal(4)>M.transcendTotal(3));const make=r=>M.derive({cursedEnergy:r,control:r,efficiency:r,martial:r,body:r,talent:r,techniquePower:r});const b=make("B"),s=make("S"),sss=make("SSS"),ex=make("EX");assert.equal(b.maxHp,268);assert.equal(b.maxCe,144);assert.equal(s.maxHp,332);assert.equal(s.maxCe,202);assert.equal(sss.maxHp,419);assert.equal(sss.maxCe,292);assert.equal(ex.maxHp,552);assert.equal(ex.maxCe,446);const exPlus=M.derive({cursedEnergy:"EX",control:"EX",efficiency:"EX",martial:"EX",body:"EX",talent:"EX",techniquePower:"EX",transcendence:{body:3,martial:3,cursedEnergy:3}});assert.ok(exPlus.maxHp>ex.maxHp);assert.ok(exPlus.maxCe>ex.maxCe);const low=M.derive({cursedEnergy:"E-",control:"B",efficiency:"E-",martial:"B",body:"B",talent:"B",techniquePower:"EX"}),high=M.derive({cursedEnergy:"E-",control:"B",efficiency:"EX",martial:"B",body:"B",talent:"B",techniquePower:"EX"});assert.equal(low.maxCe,high.maxCe);assert.ok(high.ceCostMultiplier<low.ceCostMultiplier);assert.equal(M.derive({cursedEnergy:"EX",control:"EX",efficiency:"EX",martial:"EX",body:"EX",talent:"EX",techniquePower:"EX",zeroCe:true}).maxCe,0);assert.ok(M.bodyResistance(2)<M.bodyResistance(4));assert.ok(M.bodyResistance(4)<M.bodyResistance(8));assert.ok(M.bodyResistance(20)<.42);assert.equal(M.projectionRatio("normal"),.72);assert.equal(M.projectionRatio("high"),.80);assert.equal(M.projectionRatio("ultimate"),.90);const d500=M.softBurstDamage(500,400,"normal",6,6),d1000=M.softBurstDamage(1000,400,"normal",6,6);assert.ok(d500>0&&d500<500);assert.ok(d1000>d500,"soft burst must not be a hard cap");assert.ok(M.softBurstDamage(500,400,"normal",10,6)>M.softBurstDamage(500,400,"normal",6,6));assert.equal(M.tierFromText("虚式 茈"),"ultimate");assert.equal(M.tierFromText("高输出术式"),"high");assert.equal(M.tierFromText("普通拳击"),"normal");console.log("v7-model tests passed");